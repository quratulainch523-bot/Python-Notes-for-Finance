# Dictionary and Sets

**Day 04:**

Number 01:

- I learnt dictionary in Python.
- In dictionary, “{}”, curly brackets are used.
- Key value can be anything except list because lists are mutable.
- We can print even only one item in the dictionary.


- <img width="1366" height="738" alt="image" src="https://github.com/user-attachments/assets/737d4f7f-411f-4ebd-87fc-0e1b6426f550" />

Number 02:

- We can make amendments in our input in dictionary function.
- We can add not only “value” but “key” also.
- If we do not pass any value in the brackets, then output will be a null dictionary.
- We can add value in the null dictionary.


<img width="1365" height="732" alt="image" src="https://github.com/user-attachments/assets/8e15905d-8c8e-420c-9f48-c9b78f075a9d" />


Number 03:

- We can pass “values” in the “values of keys”.
- We can also convert our tuple into a list.


<img width="1366" height="735" alt="image" src="https://github.com/user-attachments/assets/11cfef17-7f99-4721-997b-761dd1fcc36b" />


Number 04:

- I learnt how to use different functions at a time.
- I used index, length method and find lengths and values at different indices in my input.


<img width="1366" height="745" alt="image" src="https://github.com/user-attachments/assets/8a3b7b07-266b-4c55-9090-6eb6c3dab6cf" />


Number 05:

- If we try to print that value which does not exists in our input, then output will be “None”.
- If we use the key which has been already used, then the value of that “key” will be replaced by the new value.
- List and dictionary both are mutable ad cannot be stored in a set.
- We can also update our dictionary.
    
    


<img width="1366" height="730" alt="image" src="https://github.com/user-attachments/assets/b08ce792-73ba-4909-af79-43cb7755f581" />


Number 06:

- Set values are written in “{}”.
- Dictionary is unorder.
- In dictionary, outputs are printed unorderly.
- Duplicated value is count only once.
- Empty set can be created.


<img width="1365" height="722" alt="image" src="https://github.com/user-attachments/assets/462f1f42-1e9f-4654-83ec-e4297018381e" />


Number 07:

- Set ⇒ mutable.
- Elements in set ⇒ immutable.
- We can “add” any value in a set, we want.
- In the same way, we can remove any value also.
- Union ⇒ all values with zero duplication.
- Intersection ⇒ Only duplicated value.


<img width="1366" height="742" alt="image" src="https://github.com/user-attachments/assets/2f2a7f34-10f9-4e9d-a00a-39e0bc138689" />


Number 08:

- Then I started some practice questions.

- <img width="1366" height="739" alt="image" src="https://github.com/user-attachments/assets/47a02d8d-d9c9-4878-b570-67423df987db" />
